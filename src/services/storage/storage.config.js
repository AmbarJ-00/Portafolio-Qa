// Decoupled Storage Configuration
// 'localStorage' is active, 'database' is deactivated but prepared.
export const storageConfig = {
  activeProvider: 'localStorage', // Options: 'localStorage' | 'database'
};
